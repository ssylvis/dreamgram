%h2 Bucketlist
%p= link_to 'Add Dream', new_dream_path
%table
  - @dreams.each_with_index do |dream, index|
    %tr
      %td= index + 1
      %td= link_to dream.name, dream, :class => "dream"
      %td= '|'
      %td= link_to 'Destroy', dream, :method => :delete